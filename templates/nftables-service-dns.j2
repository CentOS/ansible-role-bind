# dns rules, defined by bind_recursion_acl_list
{% if bind_allow_recursion %}
{% for entry in bind_recursion_acl_list | ipv4 %}
add rule inet filter INPUT ct state new ip saddr {{ entry }} tcp dport 53 counter accept
add rule inet filter INPUT ct state new ip saddr {{ entry }} udp dport 53 counter accept
{% endfor %}
{% endif %}

